<div class="container-fluid">
  <div class="row">
  <div class="col-sm-12">
  <div class="card mb-3"id="card">
  <div class="col-md-8">
  <div class="card-body">
      <%#link_to"buy now",'#',class:"btn btn-success"%>

     <%=image_tag(@product.avatar,class:"IMG")%>
  <h5 class="card-title"><span class="">name: </span><%= @product.name %></h5>
  <p class="card-text"><span class="">price: </span><%= @product.price %></p>
  <p class="card-text"><span class="">description:</span><%= @product.description %></p>
  <% if current_user.present? && current_user.role == "admin"%>
   <%=link_to"Edit",edit_product_path,class:"btn btn-primary"%>
  <%end%>
  <%= link_to "Backs",root_path,class:"btn btn-secondary"%>
 
  </div>
  </div>
  </div>
  </div>
</div>
</div>
